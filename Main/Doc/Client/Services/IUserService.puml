@startuml
interface IUserService {
    + GetList(page:int, pageSize:int, sortBy:string, order:SortDirection, search:string, cancellationToken:CancellationToken) : Task<PaginationList<UserDTO>>
    + UpdateUserRole(dto:UserDTO) : Task
    + Delete(id:Guid) : Task
    + Add(user:UserDTO) : Task
    + Upgrade(id:Guid) : Task
    + RefreshToken(oldToken:string) : Task<string>
    + GetById(id:Guid) : Task<UserDTO?>
}
@enduml
